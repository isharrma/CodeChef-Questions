#include <bits/stdc++.h>
using namespace std;

int main() {
	int n,x;
	vector<int> a;
	cin>>n;
	for(int i=0;i<n;i++)
	{
	    cin>>x;
	    a.push_back(x);
	}
	sort(a.begin(),a.end());
	for(int j=0;j<n;j++)
	{
	    cout<<a[j]<<endl;
	}
	return 0;
}
