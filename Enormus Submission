#include <bits/stdc++.h> 
using namespace std;
int main() {
    int n,div,num,rem,i;
    cin>>n>>div;
    int flag=0;;
    for(i=0;i<n;i++)
    {
        cin>>num;
        rem=num%div;
        if(rem==0)
        {flag++;}
    }
    cout<<flag;
    
	return 0;
}
